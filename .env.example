# Base URL of the Django backend.
# Leave empty to use the Vite dev proxy (recommended for local development),
# which forwards /api and /ws to http://localhost:8000 as same-origin requests.
# For production, set this to your deployed backend origin.
VITE_API_BASE_URL=https://backend.smartlearners.ai
